{
  "name": "airgram-api",
  "version": "1.0.1",
  "homepage": "https://airgram.io",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "keywords": [
    "telegram",
    "tdlib typings",
    "tdlib wrapper",
    "tdlib javascript",
    "tdlib typescript"
  ],
  "scripts": {
    "clean": "rm -rf inputs && rm -rf methods && rm -rf models && rm -rf outputs && rm -f enums.* && rm -f index.*",
    "compile": "npm run clean && tsc",
    "deploy": "np --yolo",
    "lint": "tslint --project \".\"",
    "prepublishOnly": "npm run compile",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "pre-commit": "lint",
  "author": "Alexander Krisko",
  "license": "GPL-3.0",
  "repository": "https://github.com/airgram/airgram-api",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^10.11.7",
    "eslint": "^4.19.1",
    "eslint-config-standard": "^11.0.0",
    "eslint-friendly-formatter": "^3.0.0",
    "eslint-plugin-import": "^2.14.0",
    "eslint-plugin-node": "^6.0.1",
    "eslint-plugin-promise": "^3.7.0",
    "eslint-plugin-standard": "^3.0.1",
    "np": "^3.0.4",
    "tslint": "^5.11.0",
    "tslint-config-standard": "^7.1.0",
    "tslint-eslint-rules": "^5.4.0",
    "typescript": "^3.4.5"
  }
}
